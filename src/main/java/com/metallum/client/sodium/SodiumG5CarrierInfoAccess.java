package com.metallum.client.sodium;

/** Exact-version bridge used before a freshly built section info object is published. */
public interface SodiumG5CarrierInfoAccess {
    void metallum$mergeG5CarrierMasks(int packedMasks);
}
